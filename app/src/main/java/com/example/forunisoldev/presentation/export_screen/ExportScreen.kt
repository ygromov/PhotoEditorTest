package com.example.forunisoldev.presentation.export_screen

import androidx.compose.foundation.layout.Column
import androidx.compose.material3.Button
import androidx.compose.material3.Text
import androidx.compose.runtime.Composable

@Composable
fun ExportScreen() {
    Column() {
        Button(onClick = { /*TODO*/ }) {
            Text(text = "for Gallery")
        }
        Button(onClick = { /*TODO*/ }) {
            Text(text = "for foreign place")
        }
    }
}